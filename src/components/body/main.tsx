import { Component } from 'react';
import PokemonCard from './pokemonCard';

class Main extends Component {
  render() {
    return (
      <main>
        <PokemonCard />
      </main>
    );
  }
}

export default Main;
